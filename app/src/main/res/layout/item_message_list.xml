<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <ImageView
        android:layout_margin="10dp"
        android:id="@+id/icon_message_list"
        android:src="@drawable/head_icon"
        android:layout_marginLeft="10dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="40dp"
        android:layout_height="40dp"/>
    <ImageView
        android:id="@+id/message_list_red_point"
        app:layout_constraintTop_toTopOf="@id/icon_message_list"
        app:layout_constraintLeft_toLeftOf="@id/icon_message_list"
        android:layout_width="7dp"
        android:layout_height="7dp"
        android:src="@drawable/point_red"
        android:visibility="gone"/>
    <TextView
        android:id="@+id/tv_message_name"
        app:layout_constraintTop_toTopOf="@id/icon_message_list"
        app:layout_constraintLeft_toRightOf="@id/icon_message_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        tools:text="胡翔"
        android:text="#FFF"
        android:textSize="14dp"/>
    <TextView
        android:id="@+id/tv_message_content"
        app:layout_constraintBottom_toBottomOf="@id/icon_message_list"
        app:layout_constraintLeft_toRightOf="@id/icon_message_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:maxLines="1"
        android:maxWidth="180dp"
        tools:text="你好"
        android:textSize="12dp"
        android:ellipsize="end"/>
    <TextView
        android:id="@+id/tv_message_date"
        android:layout_marginRight="20dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        tools:text="2020-12-4"
        android:textSize="12dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>